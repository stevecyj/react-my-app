// import math from './math';

// console.log(math.square(11));

const point = [1, 2, 3];
const x = point[0];
const y = point[1];
const z = point[2];

console.log(x);

const message = 'I am ' + age + ' years old';

const message = `I am ${age} years old

`;
